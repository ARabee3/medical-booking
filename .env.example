# Environment Variables

# API Base URL (used when connecting to Django backend)
VITE_API_BASE_URL=/api

# Enable mock API (true for React phase, false when Django is ready)
VITE_MOCK_API_ENABLED=true
