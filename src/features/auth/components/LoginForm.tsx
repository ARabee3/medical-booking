import { FC } from 'react';

export const LoginForm: FC = () => {
  return (
    <div className="text-center p-8">
      <p className="text-[var(--color-muted-foreground)]">LoginForm component — implemented by Ahmed (US-001)</p>
    </div>
  );
};
