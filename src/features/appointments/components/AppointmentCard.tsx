import { FC } from 'react';

export const AppointmentCard: FC = () => {
  return (
    <div className="text-center p-8">
      <p className="text-[var(--color-muted-foreground)]">
        AppointmentCard component — implemented by Gerges (US-015)
      </p>
    </div>
  );
};
