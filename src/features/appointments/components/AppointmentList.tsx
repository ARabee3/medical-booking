import { FC } from 'react';

export const AppointmentList: FC = () => {
  return (
    <div className="text-center p-8">
      <p className="text-[var(--color-muted-foreground)]">
        AppointmentList component — implemented by Gerges (US-014)
      </p>
    </div>
  );
};
